services:
  - type: web
    name: blood-cell-classifier
    env: python
    buildCommand: pip install -r requirements-render-cpu.txt
    startCommand: gunicorn app.app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
    healthCheckPath: /health
    autoDeploy: true
